import { useRoutes } from 'react-router-dom';
// import routes from '@/routes/index';

const App = () => {
  // const routing = useRoutes(routes);
  return (
    <ul> <li className='text-2xl text-red-500 font-medium'>App</li></ul>
  )
}

export default App;
